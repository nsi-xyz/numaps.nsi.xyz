{
  "name": "numaps-nsi-xyz",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy",
    "d1:init": "wrangler d1 execute numaps-nsi-xyz --local --file=schema.sql",
    "d1:init:remote": "wrangler d1 execute numaps-nsi-xyz --remote --file=schema.sql"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.2.0",
    "@astrojs/tailwind": "^5.1.5",
    "astro": "^5.4.1",
    "hono": "^4.7.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.3",
    "wrangler": "^3.109.2"
  }
}
